'use strict';
/* DIVA frontend config.
 *
 * Leave JARVIS_API_BASE empty to use the built-in browser voice (current
 * behaviour on GitHub Pages — nothing else required).
 *
 * After you deploy server.js somewhere (Render/Railway/Fly/your own host),
 * paste that URL here and commit. The static site will then use the hosted
 * backend for live Claude answers AND the ElevenLabs human DIVA voice.
 *
 *   window.JARVIS_API_BASE = 'https://your-app.onrender.com';
 */
window.JARVIS_API_BASE = '';
